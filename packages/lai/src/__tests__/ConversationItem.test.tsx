// Placeholder test file left intentionally to avoid past transform/duplicate issues.
// The real, authoritative ConversationItem unit tests are in
// `src/__tests__/ConversationItem.unit.test.tsx`.
// This suite is skipped so Vitest does not treat this file as an empty test file.
describe.skip("ConversationItem (placeholder)", () => {
  it("noop", () => {
    // placeholder
  });
});
